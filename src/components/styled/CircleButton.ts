import styled from "styled-components";

const CircleButton = styled.button`
    display: block;
    width: 3rem;
    height: 3rem;
    font-size: 1.5rem;
    user-select: none;
    border-radius: 50%;
    border: none;
    outline: none;
`;

export default CircleButton;
