import React from "react";
import { StyledAddFieldButton } from "./styled";

const AddFieldButton: React.FC = () => (
    <StyledAddFieldButton>+</StyledAddFieldButton>
);

export default AddFieldButton;
