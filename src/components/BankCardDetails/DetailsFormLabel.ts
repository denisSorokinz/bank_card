import styled from "styled-components";
import colors from "Assets/colors";

const DetailsFormLabel = styled.label`
    display: inline-block;
    color: ${colors.darkGreen};
    margin-bottom: .75rem;
`;

export default DetailsFormLabel;
