import { createContainer } from "unstated-next";
import { useAddFieldModal } from "Hooks";

const AddFieldModalStateContainer = createContainer(useAddFieldModal);

export default AddFieldModalStateContainer;
